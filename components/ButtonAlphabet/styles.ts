import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 30,
    height: 30,
    padding: 2,
    borderColor: "#000000",
    borderRadius: 6,
    borderWidth: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
