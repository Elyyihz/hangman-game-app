import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  rightLeg: {
    width: 1,
    height: 50,
    borderColor: "#000",
    borderWidth: 1,
    right: 12,
    bottom: 6,
    transform: [{ rotate: "30deg" }],
  },
});
